<UserControl x:Class="Laundry.Views.OrderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Laundry.Views"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Search & Filter -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Cari:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" x:Name="SearchTextBox" Margin="0,0,10,0" TextChanged="SearchTextBox_TextChanged"/>
            
            <TextBlock Grid.Column="2" Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="3" x:Name="StatusComboBox" Width="150" Margin="0,0,10,0" SelectionChanged="StatusComboBox_SelectionChanged"/>
            
            <Button Grid.Column="4" Content="Refresh" Click="RefreshButton_Click" Width="100"/>
        </Grid>
        
        <!-- Orders List -->
        <DataGrid Grid.Row="1" x:Name="OrdersDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="60"/>
                <DataGridTextColumn Header="Tanggal" Binding="{Binding OrderDate, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}" Width="150"/>
                <DataGridTextColumn Header="Pelanggan" Binding="{Binding Customer.Name}" Width="180"/>
                <DataGridTextColumn Header="Total" Binding="{Binding FinalAmount, StringFormat='{}{0:N0}'}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Pembayaran" Binding="{Binding IsPaid, Converter={StaticResource BoolToStatusConverter}}" Width="100"/>
                <DataGridTemplateColumn Header="Aksi" Width="260">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Detail" Click="DetailButton_Click" Tag="{Binding OrderId}" Width="60" Margin="0,0,5,0"/>
                                <Button Content="Pembayaran" Click="PaymentButton_Click" Tag="{Binding OrderId}" Width="90" Margin="0,0,5,0"
                                        Visibility="{Binding IsPaid, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}"/>
                                <Button Content="Update Status" Click="UpdateStatusButton_Click" Tag="{Binding OrderId}" Width="90"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>