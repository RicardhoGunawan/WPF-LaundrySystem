
<UserControl x:Class="Laundry.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Laundry.Views"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Search & Actions -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Cari:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" x:Name="SearchTextBox" Margin="0,0,10,0" TextChanged="SearchTextBox_TextChanged"/>
            
            <Button Grid.Column="2" Content="Refresh" Click="RefreshButton_Click" Width="100" Margin="0,0,10,0"/>
            <Button Grid.Column="3" Content="Tambah Pelanggan" Click="AddCustomerButton_Click" Width="150"/>
        </Grid>
        
        <!-- Customers List -->
        <DataGrid Grid.Row="1" x:Name="CustomersDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="60"/>
                <DataGridTextColumn Header="Nama" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="No. Telepon" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Alamat" Binding="{Binding Address}" Width="180"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="150"/>
                <DataGridTextColumn Header="Terdaftar" Binding="{Binding RegisterDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="100"/>
                <DataGridTemplateColumn Header="Aksi" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="EditButton_Click" Tag="{Binding CustomerId}" Width="60" Margin="0,0,5,0"/>
                                <Button Content="Hapus" Click="DeleteButton_Click" Tag="{Binding CustomerId}" Width="60" 
                                        Background="#e74c3c"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>