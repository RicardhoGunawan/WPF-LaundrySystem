<UserControl x:Class="Laundry.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Laundry.Views"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Laporan Laundry" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <!-- Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Periode:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="1" x:Name="PeriodComboBox" Width="150" Margin="0,0,20,0" SelectionChanged="PeriodComboBox_SelectionChanged"/>
            
            <TextBlock Grid.Column="2" Text="Dari:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker Grid.Column="3" x:Name="StartDatePicker" Width="120" Margin="0,0,10,0"/>
            
            <TextBlock Grid.Column="4" Text="Sampai:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker Grid.Column="5" x:Name="EndDatePicker" Width="120" Margin="0,0,20,0"/>
            
            <Button Grid.Column="7" Content="Generate Laporan" Click="GenerateReportButton_Click" Width="150"/>
        </Grid>
        
        <!-- Report Tabs -->
        <TabControl Grid.Row="2" x:Name="ReportTabControl">
            <TabItem Header="Ringkasan">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
                        <TextBlock Text="Ringkasan Periode" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                        <WrapPanel>
                            <Border Background="#3498db" Width="200" Height="100" Margin="0,0,10,10" CornerRadius="5">
                                <StackPanel Margin="10" VerticalAlignment="Center">
                                    <TextBlock Text="Total Pesanan" Foreground="White"/>
                                    <TextBlock x:Name="TotalOrdersTextBlock" Text="0" FontSize="24" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Background="#2ecc71" Width="200" Height="100" Margin="0,0,10,10" CornerRadius="5">
                                <StackPanel Margin="10" VerticalAlignment="Center">
                                    <TextBlock Text="Total Pendapatan" Foreground="White"/>
                                    <TextBlock x:Name="TotalRevenueTextBlock" Text="Rp 0" FontSize="24" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Background="#e74c3c" Width="200" Height="100" Margin="0,0,10,10" CornerRadius="5">
                                <StackPanel Margin="10" VerticalAlignment="Center">
                                    <TextBlock Text="Pesanan Belum Dibayar" Foreground="White"/>
                                    <TextBlock x:Name="UnpaidOrdersTextBlock" Text="0" FontSize="24" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Background="#9b59b6" Width="200" Height="100" Margin="0,0,10,10" CornerRadius="5">
                                <StackPanel Margin="10" VerticalAlignment="Center">
                                    <TextBlock Text="Pesanan Selesai" Foreground="White"/>
                                    <TextBlock x:Name="CompletedOrdersTextBlock" Text="0" FontSize="24" FontWeight="Bold" Foreground="White"/>
                                </StackPanel>
                            </Border>
                        </WrapPanel>
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" x:Name="SummaryDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tanggal" Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}" Width="120"/>
                            <DataGridTextColumn Header="Jumlah Pesanan" Binding="{Binding OrderCount}" Width="120"/>
                            <DataGridTextColumn Header="Total (Rp)" Binding="{Binding TotalAmount, StringFormat='{}{0:N0}'}" Width="150"/>
                            <DataGridTextColumn Header="Rata-rata (Rp)" Binding="{Binding AverageAmount, StringFormat='{}{0:N0}'}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            
            <TabItem Header="Layanan">
                <DataGrid x:Name="ServiceReportDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nama Layanan" Binding="{Binding ServiceName}" Width="*"/>
                        <DataGridTextColumn Header="Jumlah Pesanan" Binding="{Binding OrderCount}" Width="120"/>
                        <DataGridTextColumn Header="Total Berat (Kg)" Binding="{Binding TotalWeight, StringFormat='{}{0:N1}'}" Width="120"/>
                        <DataGridTextColumn Header="Total Pendapatan (Rp)" Binding="{Binding TotalRevenue, StringFormat='{}{0:N0}'}" Width="150"/>
                        <DataGridTextColumn Header="Persentase" Binding="{Binding Percentage, StringFormat='{}{0:P1}'}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <TabItem Header="Pelanggan">
                <DataGrid x:Name="CustomerReportDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nama Pelanggan" Binding="{Binding CustomerName}" Width="*"/>
                        <DataGridTextColumn Header="Jumlah Pesanan" Binding="{Binding OrderCount}" Width="120"/>
                        <DataGridTextColumn Header="Total Pengeluaran (Rp)" Binding="{Binding TotalSpent, StringFormat='{}{0:N0}'}" Width="150"/>
                        <DataGridTextColumn Header="Rata-rata per Pesanan (Rp)" Binding="{Binding AveragePerOrder, StringFormat='{}{0:N0}'}" Width="170"/>
                        <DataGridTextColumn Header="Pesanan Terakhir" Binding="{Binding LastOrderDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        
        <!-- Export Button -->
        <Button Grid.Row="3" Content="Print Laporan" Click="PrintButton_Click" 
                HorizontalAlignment="Right" Width="150" Margin="0,10,0,0"/>
    </Grid>
</UserControl>